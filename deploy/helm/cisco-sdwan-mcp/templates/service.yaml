apiVersion: v1
kind: Service
metadata:
  name: {{ include "cisco-sdwan-mcp.fullname" . }}
  labels:
    {{- include "cisco-sdwan-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "cisco-sdwan-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
